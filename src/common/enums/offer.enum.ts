export enum EnergyType {
  ELECTRICITY = 'electricity',
  GAS = 'gas',
  DUAL = 'dual',
}

export enum MarketType {
  FIXED = 'fixed',
  VARIABLE = 'variable',
  INDEXED = 'indexed',
}

export enum UserTarget {
  PERSONAL = 'personal',
  BUSINESS = 'business',
  BOTH = 'both',
}
